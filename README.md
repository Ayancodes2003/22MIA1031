# 22MIA1031 Backend Evaluation

## Setup
1. Create a virtual environment: `python -m venv venv`
2. Activate the virtual environment.
3. Install dependencies: `pip install -r vehicle_maintenance_scheduler/requirements.txt`
4. Fill in `.env` with appropriate credentials.

## Run
`uvicorn vehicle_maintenance_scheduler.main:app --reload`